{
  "name": "union-server",
  "version": "1.0.0",
  "description": "Lightweight chat service built with NodeJS",
  "main": "build/index.js",
  "scripts": {
    "getui": "git clone https://github.com/Union-Chat/union-react && cd union-react && npm i --prod false && npm run prod && cd .. && rm -rf dist && mv union-react/index.html index.html && mv union-react/dist dist && rm -rf union-react",
    "lint": "eslint **/*.js --ignore-pattern build/* --ignore-pattern dist/* --ignore-pattern docs/*",
    "start": "cross-env NODE_ENV=production node src/index.js",
    "dev": "nodemon src/index.js --use-insecure"
  },
  "keywords": [],
  "author": "Kromatic",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.4",
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "express-rate-limit": "^3.4.0",
    "ioredis": "^4.6.2",
    "mongodb": "^3.1.13",
    "node-fetch": "^2.2.0",
    "prompt": "^1.0.0",
    "rate-limit-redis": "^1.6.0",
    "shortid": "^2.2.12",
    "ws": "^6.1.4"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "eslint": "^5.14.1",
    "nodemon": "^1.18.10"
  }
}
